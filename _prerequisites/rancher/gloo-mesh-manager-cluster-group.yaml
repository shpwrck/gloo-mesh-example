apiVersion: fleet.cattle.io/v1alpha1
kind: ClusterGroup
metadata:
  name: gloo-mesh-manager
  namespace: fleet-default
spec:
  selector:
    matchExpressions: []
    matchLabels:
      gloo-mesh.cluster-type: manager
